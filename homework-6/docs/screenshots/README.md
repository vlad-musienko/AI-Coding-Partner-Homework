Place pipeline screenshots here.

Required:
- pipeline-run.png       — terminal output of `python integrator.py`
- test-coverage.png      — pytest coverage report (≥ 80%)
- skill-run-pipeline.png — /run-pipeline slash command executing in Claude Code
- hook-trigger.png       — coverage gate hook firing (blocking a push)
- mcp-interaction.png    — context7 query result + custom MCP tool call
